

const getStudentInfo = (req, res) => {
  console.log("Retrieve student info with studentid ", req.params.studentid);
  // return res.send("Hello World with studenid", 200);
  return
};

module.exports = {
  getStudentInfo
};
